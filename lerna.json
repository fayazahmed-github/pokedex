{
    "$schema": "node_modules/lerna/schemas/lerna-schema.json",
    "version": "independent",
    "npmClient": "yarn",
    "packages": ["apps/*", "packages/*"],
    "command": {
      "publish": {
        "ignoreChanges": ["*.md"]
      }
    }
  }
  